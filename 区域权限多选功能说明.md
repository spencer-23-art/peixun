# 区域权限多选功能 · 修改说明

**修改日期**：2026-08-21  
**需求**：管理端（手机 Web + PC）筛选增加"区域权限"筛选项；一人可拥有多个区域权限（工人记录 + 管理端账号）

---

## 一、功能概览

| 能力 | 说明 |
|------|------|
| 工人记录多区域 | 一条培训记录可属于多个区域，`region_auth` 逗号分隔存储（如 `三元肥,尿素塔`） |
| 管理端账号多区域 | 二级管理员可绑定多个区域，登录后只能看到自己负责区域的记录 |
| 区域权限筛选 | 管理端记录列表筛选器新增"区域权限"多选（手机 + PC 同步） |
| 权限交集规则 | 筛选项与账号权限取交集：账号权限 [A,B]，筛选 [B,C] → 只出 B 区域 |

## 二、权限规则

- **超级管理员 admin**：`region_auth` 为空 → 看全部记录
- **二级管理员**：`region_auth` 非空 → 后端强制只返回权限区域内记录；空 → 看全部
- **普通录入员**（user 角色）：不受区域限制（与现状一致）

## 三、后端改动（main.py）

| 文件位置 | 改动 |
|---------|------|
| `init_db()` | users 表自愈新增 `region_auth` 列（逗号分隔多区域） |
| 新增 `_parse_region_list()` | 逗号分隔区域字符串 → 去空列表 |
| 新增 `_region_match_conditions()` | 生成 `','||col||',' LIKE '%,区域,%'` 精确匹配 SQL |
| `/api/admin/records` | 新增 `regions` 参数（逗号分隔多选）+ 账号权限强制过滤（取交集） |
| `/api/admin/record/update_region` | 支持逗号分隔多区域写入（自动去空白、去重） |
| `/api/admin/add_sub_admin` | 新增 `region_auth` 参数（创建时绑定区域） |
| `/api/admin/update_sub_admin` | 新增 `region_auth` 参数（修改区域，空串=清空为全部区域） |
| `/api/admin/sub_admins` | 列表返回 `region_auth` 字段 |
| 登录接口 | 返回 `region_auth` 字段 |
| `build_gate_csv` | 门禁导出多区域记录取第一个区域（门禁每行只认一个组织路径） |

## 四、前端改动（static/admin.html）

| 位置 | 改动 |
|-----|------|
| CSS | 新增 `.region-check` 多选标签样式、`.filter-region-box` 容器样式 |
| 记录筛选器卡片 | 新增"区域权限"多选控件（`filter-regions-box`，按 `systemRegions` 动态渲染） |
| `loadRecords()` | 传 `regions` 参数（逗号分隔选中区域） |
| `renderRegionFilterOptions()` | 渲染筛选器区域多选（新增） |
| `handleRegionFilterChangeByIdx()` | 筛选器区域切换（新增，索引取值防转义） |
| `clearDateFilter()` | 重置时同时清空区域筛选 |
| 记录表格每行 | 区域权限从单选下拉改为多选标签（`row-regions-${id}`）+ 保存按钮 |
| `handleRowRegionToggle()` | 行内区域勾选显示保存按钮（新增） |
| `updateRecordRegion()` | 收集所有勾选区域逗号分隔提交（改造） |
| 二级管理员列表 | 新增"区域权限"列（多区域标签展示） |
| 二级管理员弹窗 | 新增"区域权限"多选（`sub-admin-regions-box`） |
| `renderSubAdminRegionOptions()` / `handleSubAdminRegionToggle()` | 弹窗区域多选逻辑（新增） |
| `submitSubAdmin()` | 提交 `region_auth`（逗号分隔） |

## 五、数据兼容

- 现有单区域记录（如 `三元肥`）无需迁移，`','||'三元肥'||',' LIKE '%,三元肥,%'` 直接命中
- 新多区域记录写入时自动去空白、去重
- users 表新增列由自愈逻辑创建，历史库启动即生效

## 六、验证结果

| 测试项 | 结果 |
|--------|------|
| `records?regions=三元肥` 筛选 | ✅ total=19 |
| `records?regions=三元肥,尿素塔` 多选 | ✅ total=19 |
| 二级管理员（区域=三元肥）登录可见范围 | ✅ 仅 19 条三元肥记录 |
| 权限交集（账号三元肥，筛尿素塔） | ✅ total=0（交集为空） |
| 记录写多区域"三元肥,尿素塔"后筛尿素塔 | ✅ total=1（命中） |
| `update_sub_admin` 设置多区域后重新登录 | ✅ region_auth=三元肥,尿素塔 |
| `sub_admins` 返回 region_auth 字段 | ✅ |
| 登录返回 region_auth 字段 | ✅ |
| admin.html JS 语法（node 检查） | ✅ 通过 |
| main.py 语法编译 | ✅ 通过 |
| 测试数据已清理 | ✅ 记录区域恢复、测试账号已删除、备份已清理 |

## 七、使用说明

1. **给工人设置多区域**：管理端记录列表 → 该行"权限"处勾选多个区域 → 点"保存"
2. **给二级管理员绑定区域**：系统设置 → 二级管理员账户管理 → 修改 → 勾选区域 → 确定（不勾选 = 全部区域）
3. **按区域筛选**：记录列表筛选器 → 区域权限 → 勾选一个或多个区域（手机端同位置）
4. **权限生效**：受限二级管理员登录后，列表自动只显示其权限区域内记录，筛选权限外区域结果为空
