# 代码审核报告

**项目名称**：心连心培训信息录入系统  
**审核日期**：2026-07-10  
**审核范围**：安全审计 + 死代码/冗余代码识别  
**审核原则**：所有建议均确保不影响现有功能  

---

## 一、安全审计

### 🔴 [高危] S1. 默认管理员硬编码弱密码

**位置**：`main.py:448-458`  
**类型**：CWE-798 硬编码凭证  
**置信度**：高

**问题**：`init_db()` 中硬编码了两个默认管理员账号，密码分别为 `admin123` 和 `admin1234`，均为极弱密码。任何人知道源码即可登录管理员账户，获取全部人员身份证信息、照片和考试成绩。

```python
# main.py:448
cursor.execute(
    "INSERT INTO users (...) VALUES (?, ?, ?, ?, ?, ?)",
    ('admin', encrypt_pwd('admin123'), '系统管理员', '管理部', 'approved', 'admin')
)
cursor.execute(
    "INSERT INTO users (...) VALUES (?, ?, ?, ?, ?, ?)",
    ('admin2', encrypt_pwd('admin1234'), '普通管理员', '管理部', 'approved', 'admin')
)
```

**修复建议**（不影响功能）：
- 首次启动时从环境变量 `PEIXUN_ADMIN_PASSWORD` 读取密码，未设置则生成随机密码并打印到控制台
- `admin2` 账号改为不预创建，由超级管理员通过界面添加

```python
import secrets, string
admin_pwd = os.environ.get("PEIXUN_ADMIN_PASSWORD")
if not admin_pwd:
    admin_pwd = ''.join(secrets.choice(string.ascii_letters + string.digits) for _ in range(16))
    print(f"[INIT] 管理员初始密码: {admin_pwd}（请立即修改）")
cursor.execute(
    "INSERT INTO users (...) VALUES (?, ?, ?, ?, ?, ?)",
    ('admin', encrypt_pwd(admin_pwd), '系统管理员', '管理部', 'approved', 'admin')
)
```

---

### 🟡 [中危] S2. CORS 配置过于宽松

**位置**：`main.py:613-618`  
**类型**：CWE-942 跨域资源共享配置过宽  
**置信度**：高

**问题**：`allow_origins=["*"]` 允许任意网站发起请求。虽然 `allow_credentials=False`（Token 通过 Header 传递而非 Cookie），但恶意网站仍可通过 JS 携带用户 Token 发起请求。

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],        # ← 任意来源
    allow_credentials=False,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

**修复建议**（不影响功能）：
- 如系统仅在局域网使用，限制为 `["http://局域网IP:8000"]` 或从环境变量读取
- 如需多端访问，至少限制为已知的前端域名列表

```python
_allowed_origins = os.environ.get("PEIXUN_CORS_ORIGINS", "*").split(",")
app.add_middleware(
    CORSMiddleware,
    allow_origins=_allowed_origins,
    allow_credentials=False,
    allow_methods=["GET", "POST", "PUT", "DELETE"],
    allow_headers=["*"],
)
```

---

### 🟡 [中危] S3. OCR 上传接口无文件大小限制

**位置**：`main.py:1034-1042`（`/api/ocr_idcard`）、`main.py:1073-1095`（`/idcard/ocr`）、`app/main.py:13-42`  
**类型**：CWE-400 拒绝服务  
**置信度**：高

**问题**：三个 OCR 上传接口中，仅 `/idcard/ocr` 做了扩展名白名单校验，但均未限制文件大小。攻击者可上传超大文件耗尽磁盘空间导致服务崩溃。对比 `/api/record` 接口已正确限制 5MB。

```python
# main.py:1042 — 无大小限制
with open(temp_path, "wb") as f:
    shutil.copyfileobj(file.file, f)  # 直接拷贝全部内容
```

**修复建议**（不影响功能）：
```python
MAX_OCR_FILE_SIZE = 10 * 1024 * 1024  # 10MB

content = await file.read(MAX_OCR_FILE_SIZE + 1)
if len(content) > MAX_OCR_FILE_SIZE:
    raise HTTPException(status_code=400, detail="图片文件大小不能超过10MB")
await file.seek(0)

with open(temp_path, "wb") as f:
    f.write(content)
```

---

### 🟡 [中危] S4. 错误信息泄露服务器内部细节

**位置**：`main.py:1095`、`main.py:48-62`  
**类型**：CWE-209 信息泄露  
**置信度**：高

**问题**：OCR 接口将内部异常堆栈和依赖信息直接返回给客户端，暴露服务器环境。

```python
# main.py:1095
raise HTTPException(status_code=500, detail=f"Failed to submit task: {str(e)}")

# main.py:56 — 将 OCR 依赖错误原因返回给客户端
raise HTTPException(status_code=500, 
    detail=f"OCR 引擎依赖缺失...错误原因: {OCR_ERROR_MSG}")
```

**修复建议**（不影响功能）：
```python
# 对外返回通用错误，内部细节仅打印到日志
print(f"[Error] OCR submit failed: {str(e)}")
raise HTTPException(status_code=500, detail="图片处理失败，请重试")
```

---

### 🟡 [中危] S5. 考试身份验证接口无频率限制

**位置**：`main.py:2087-2158`（`/api/exam/verify`）  
**类型**：CWE-307 认证枚举  
**置信度**：中

**问题**：考试验证仅凭姓名 + 身份证后6位，无任何频率限制。身份证后6位空间仅 100 万种组合，可被短时间内暴力穷举。

**修复建议**（不影响功能）：
- 基于内存的简单频率限制：同一 IP 5 分钟内最多 10 次验证
- 超过限制返回 429

```python
from collections import defaultdict
from time import time

_verify_attempts = defaultdict(list)
_VERIFY_WINDOW = 300  # 5分钟
_VERIFY_MAX = 10      # 最多10次

def _check_verify_rate(client_ip: str):
    now = time()
    attempts = _verify_attempts[client_ip]
    _verify_attempts[client_ip] = [t for t in attempts if now - t < _VERIFY_WINDOW]
    if len(_verify_attempts[client_ip]) >= _VERIFY_MAX:
        raise HTTPException(status_code=429, detail="验证尝试过于频繁，请5分钟后再试")
    _verify_attempts[client_ip].append(now)
```

---

### 🔵 [低危] S6. `/api/ocr_idcard` 未校验文件扩展名

**位置**：`main.py:1039-1041`  
**类型**：CWE-434 不受限文件上传  
**置信度**：中

**问题**：`/api/ocr_idcard` 接口未做扩展名白名单校验（`/idcard/ocr` 有校验）。虽然文件仅用于 OCR 图像处理，但缺少校验不是好实践。

```python
# main.py:1039 — 无白名单
ext = os.path.splitext(file.filename)[1].lower()
if not ext:
    ext = '.jpg'
```

**修复建议**（不影响功能）：
```python
ext = os.path.splitext(file.filename)[1].lower()
if ext not in ['.jpg', '.jpeg', '.png', '.bmp']:
    ext = '.jpg'  # 默认
```

---

### 🔵 [低危] S7. 登录调试日志泄露用户名和 IP

**位置**：`main.py:761-778`  
**类型**：CWE-532 日志中插入敏感信息  
**置信度**：高

**问题**：登录接口将用户名明文和客户端 IP 打印到日志文件 `app_stdout.log`。

```python
print(f"[Login-Debug] 尝试登录 | 原始输入: username={repr(username)}, ...")
print(f"[Login-Debug] 登录失败: 用户不存在. 输入的用户名={repr(username_clean)} (IP={client_ip})")
```

**修复建议**（不影响功能）：生产环境应脱敏或移除调试日志：
```python
print(f"[Login] 登录失败 (IP={client_ip})")
```

---

### 🔵 [低危] S8. 前端题目渲染存在 XSS 风险

**位置**：`static/exam.html:789-806`  
**类型**：CWE-79 跨站脚本  
**置信度**：中

**问题**：题目文本通过 `innerHTML` 直接渲染，若题库 Excel 中包含 HTML 标签则可能执行。

```javascript
container.innerHTML = `...<p class="question-text">${question.question}</p>...`;
```

**修复建议**（不影响功能）：添加转义函数：
```javascript
function escapeHtml(text) {
    const div = document.createElement('div');
    div.textContent = text;
    return div.innerHTML;
}
// 使用时
`<p class="question-text">${escapeHtml(question.question)}</p>`
```

---

### ✅ 已正确实施的安全措施

| 措施 | 位置 | 说明 |
|------|------|------|
| SQL 参数化查询 | 全部 SQL | 所有用户输入均通过 `?` 占位符，无注入风险 |
| 密码加盐 SHA-256 | `main.py:156-159` | 使用 `uuid4` 盐 + HMAC 比较，防时序攻击 |
| Token HMAC 签名 | `main.py:172-204` | 服务端签名，有效期纳入签名不可篡改 |
| 路径穿越防护 | `main.py:270-276` | `safe_temp_id_path` 使用 `realpath` 校验 |
| 上传目录鉴权 | `main.py:642-650` | `protect_uploads` 中间件保护身份证照片 |
| 题库白名单校验 | `main.py:2411-2413` | `get_questions` 只允许配置中的文件名 |
| 日志 Token 脱敏 | `main.py:627-636` | query_params 中 token 替换为 `***` |
| 考试频率限制 | `main.py:2489-2504` | 5 分钟内同人不重复提交 |
| 身份证号校验 | `main.py:252-266` | ISO 7064 Mod 11 校验 + 生日合法性 |
| 文件名净化 | `main.py:278-284` | 导出文件名过滤非法字符 |

---

## 二、死代码与冗余代码

### D1. `app/main.py` — 整个文件为死代码 🔴

**位置**：`app/main.py`（67行）  
**影响**：定义了独立的 FastAPI 应用，包含 `/idcard/ocr` 和 `/idcard/result/{task_id}` 两个端点，但该应用从未被导入或挂载到根 `main.py`。根 `main.py` 已有完全相同功能的端点。

**处理建议**：可直接删除整个文件。根 `main.py:1073-1117` 已提供相同功能。

---

### D2. `ocr_handler.py` — 3 个未使用的函数/变量

| 名称 | 类型 | 行号 | 说明 |
|------|------|------|------|
| `OCR_ENGINE_NO_CLS` | 变量 | L31 | 定义为 None，从未被读取或赋值 |
| `_concat_model_path()` | 函数 | L19-26 | 定义后从未被调用 |
| `_anchor_orientation_score()` | 函数 | L723-762 | 定义后从未被调用 |
| `read_yaml` | 导入 | L16 | 从 `rapidocr_onnxruntime.utils` 导入但从未使用 |

**处理建议**：删除上述 4 项。不影响任何功能。

---

### D3. `exam.html` — 重复定义的空函数

**位置**：`static/exam.html:594`

```javascript
// 状态栏时钟（已移除状态栏，此函数保留为空避免报错）
function updateStatusBarTime() {}
```

**问题**：L744 又重新定义了同名函数（有实际逻辑），L594 的空定义是死代码。  
**处理建议**：删除 L593-594 的空函数定义。

---

### D4. OCR 引擎初始化代码重复

**位置**：`app/ocr.py:9-60` 与 `ocr_handler.py:43-95`

**问题**：两处几乎完全相同的代码：模型下载 → 字典文件生成 → monkey patch → RapidOCR 实例化。维护时改一处忘另一处会产生隐蔽 bug。

**处理建议**（需谨慎）：将 `app/ocr.py` 的 `get_ocr_engine()` 改为直接调用 `ocr_handler.py` 的 `init_ppocrv6()`，消除重复。但两者返回的引擎配置略有差异（一个有 `use_angle_cls=False`），需测试确认无影响后再合并。

---

### D5. 民族列表重复定义

**位置**：`app/postprocess.py:4-10`（NATIONS 集合）与 `ocr_handler.py:443`（nations 列表）

**问题**：56 个民族列表在两处分别维护，顺序和格式不同。

**处理建议**：`ocr_handler.py` 的 `extract_nation()` 改为引用 `app.postprocess.NATIONS`。

---

### D6. 配置常量重复定义

**位置**：3 个文件分别定义了相同常量

| 常量 | `main.py` | `app/config.py` | `ocr_handler.py` |
|------|-----------|-----------------|-------------------|
| `DB_PATH` | L65 | — | L32 |
| `UPLOAD_DIR` | L66 | L4 | L33 |
| `TEMP_IDS_DIR` | L67 | L5 | L34 |
| `IDCARD_SAVE_DIR` | — | L6 | L36 |

**处理建议**：统一到 `app/config.py`，其他文件引用。改动量大但无功能影响。

---

### D7. `app/detector.py` — 误导性文档注释

**位置**：`app/detector.py:5-8`

```python
def detect_idcard_corners(img_gray: np.ndarray) -> np.ndarray:
    """
    Step 2: 文档检测，寻找身份证的四个角点。
    优先尝试引入并集成 document-preprocessor，失败时自动平滑回退到 OpenCV 寻找最大外接四边形算法。
    """
```

**问题**：注释说"优先尝试 document-preprocessor"，但函数体内完全没有 document-preprocessor 相关代码，只有 OpenCV contour 检测。document-preprocessor 逻辑实际在 `app/tasks.py:43-66` 中。

**处理建议**：修正注释为实际行为描述。

---

### D8. `app/preprocess.py` — 策略注释与实现不符

**位置**：`app/tasks.py:113` vs `app/preprocess.py:38-42`

```python
# app/tasks.py:113 注释说：
# version_c: 基础预处理 + 自适应二值化

# 但实际函数 app/preprocess.py:38 做的是：
def get_strategy_c(img):
    """Strategy Version C: 彩色图像自适应对比度拉伸"""
    enhanced = cv2.convertScaleAbs(img, alpha=1.15, beta=8)  # 对比度拉伸，非二值化
```

**处理建议**：将 `app/tasks.py:113` 注释改为"基础预处理 + 对比度拉伸"。

---

### D9. `local_deps_backup/` — 1243 个备份文件

**位置**：项目根目录 `local_deps_backup/`

**问题**：包含 anyio、click、h11、colorama、fastapi 等第三方包的完整备份副本，共 1243 个文件。这些包已通过 pip 正常安装，备份目录纯属冗余，且会干扰代码搜索和 IDE 索引。

**处理建议**：删除整个目录。不影响运行时功能。

---

### D10. `scratch/` — 68 个开发测试脚本

**位置**：项目根目录 `scratch/`

**问题**：包含 43 个 .py 测试脚本、12 个 .txt、4 个 .png、2 个 .pem 证书、1 个 .mobileprovision 文件。这些是开发调试产物，不参与运行时。

**处理建议**：可归档或删除。特别注意其中 2 个 `.pem` 文件可能包含私钥，应确认安全后删除。

---

### D11. `annotated_doc/` 和 `annotated_types/` — 自动生成文件

**位置**：项目根目录

**问题**：包含 `.typed` 文件和 `.py` 注解文件，看起来是类型检查工具自动生成的。不参与运行时。

**处理建议**：可删除。如需类型检查，应通过配置 pyproject.toml 管理。

---

### D12. `__pycache__/` 目录

**位置**：项目多处

**问题**：多处 `__pycache__/` 目录包含 .pyc 编译缓存文件。

**处理建议**：添加到 `.gitignore`，可安全删除。

---

## 三、代码质量建议（非安全、非死代码）

### Q1. SQL 查询大量使用裸 `sqlite3.connect()` 而非 `get_db()` 上下文管理器

项目中定义了 `get_db()` 上下文管理器（`main.py:238-245`），但大部分接口仍手动 `conn = sqlite3.connect(...)` + `conn.close()`，存在忘记关闭连接的风险。

**影响**：无即时 bug，但维护性差。  
**建议**：后续重构统一使用 `get_db()`。

---

### Q2. `exam_verify` 和 `save_exam_record` 接收裸 `dict` 而非 Pydantic 模型

```python
@app.post("/api/exam/verify")
def exam_verify(data: dict):  # 无类型校验
```

**建议**：使用 Pydantic BaseModel 进行输入校验，FastAPI 自动返回 422 错误。

---

### Q3. `beijing_now()` 使用 `utcnow() + timedelta(hours=8)` 而非 `datetime.now(timezone(timedelta(hours=8)))`

**位置**：`main.py:217-218`

```python
def beijing_now() -> datetime:
    return datetime.utcnow() + timedelta(hours=8)
```

**问题**：`utcnow()` 返回无时区信息的 naive datetime，加 8 小时后仍为 naive。在 Python 3.12+ 中 `utcnow()` 已被标记为 deprecated。

**建议**（不影响功能）：
```python
from datetime import timezone
def beijing_now() -> datetime:
    return datetime.now(timezone(timedelta(hours=8))).replace(tzinfo=None)
```

---

## 四、处理优先级

| 优先级 | 编号 | 类型 | 说明 |
|--------|------|------|------|
| **立即处理** | S1 | 安全 | 默认弱密码 → 改为环境变量/随机密码 |
| **立即处理** | S3 | 安全 | OCR 上传无大小限制 → 加 10MB 限制 |
| **本周处理** | S2 | 安全 | CORS 通配符 → 限制来源 |
| **本周处理** | S4 | 安全 | 错误信息泄露 → 脱敏 |
| **本周处理** | D1 | 死代码 | `app/main.py` 整个文件删除 |
| **本周处理** | D9 | 死代码 | `local_deps_backup/` 删除 |
| **下周处理** | S5 | 安全 | 考试验证频率限制 |
| **下周处理** | S6 | 安全 | OCR 扩展名校验 |
| **下周处理** | D2 | 死代码 | `ocr_handler.py` 清理未使用代码 |
| **下周处理** | D10 | 死代码 | `scratch/` 归档（注意 .pem 文件） |
| **迭代优化** | S7-S8 | 安全 | 日志脱敏、XSS 转义 |
| **迭代优化** | D3-D8 | 死代码 | 注释修正、重复代码合并 |
| **迭代优化** | Q1-Q3 | 质量 | 统一 DB 连接、Pydantic 模型、时区 |

---

## 五、总结

| 维度 | 数量 | 说明 |
|------|------|------|
| 安全问题 | 8 项 | 1 高危 + 4 中危 + 3 低危 |
| 死代码 | 12 项 | 1 个完整文件 + 4 个未使用函数 + 7 项冗余 |
| 代码质量 | 3 项 | 维护性改进 |

**所有安全修复和死代码清理均可在不影响现有功能的前提下完成。** 建议按优先级逐项处理，每项修改后做一轮功能验证。
